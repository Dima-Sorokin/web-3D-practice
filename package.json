{
  "name": "3dweb",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "deploy": "npm run build && gh-pages -d build",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^2.9.9"
  },
  "dependencies": {
    "gh-pages": "^4.0.0",
    "three": "^0.140.2"
  }
}
